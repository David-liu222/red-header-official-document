---
name: red-header-official-document
description: 按已确认的中国企业红头公文规范生成、改写和复核 Word/DOCX 公文，处理红头、发文字号、标题、正文、层级序号、主送单位、附件、落款、日期、文件命名和渲染验收。Use when 用户要求“套红头”“红头文件”“公文格式”、提供红头公文样例或规则截图，或需要把请示、报告、通知等文稿排成指定格式。
---

# 套红头公文

## 核心原则

把用户提供的模板、规则截图和已确认口径视为最高约束；不凭一般公文常识替换规则，不擅自改文字、主送单位、签发类型、字号、编号或落款。遇到冲突或缺字段，先记录为“待确认”，再继续做不依赖该字段的部分。

详细固定规则、冲突边界和待补充项见 [references/rules-and-open-questions.md](references/rules-and-open-questions.md)。

## 工作流

1. 收集正文、红头模板/样例、规则截图、主送单位、发文字号、落款单位、日期、附件及输出格式；旧 `.doc` 只在副本上转换为 `.docx`，绝不覆盖源文件。网页任务来自 `task.json` 时，先读取 `payload`、`red_head_document`、`source_files`、`input_folder` 和 `output_folder`。
2. 先判断发文类型：主送外单位为外发文；主送公司各部门或迎检材料为内发文。培训等迎检文件登记“迎检资料”备注，但备注位置不明确时不得擅自塞进标题。
3. 建立字段表：文件名必须与正文标题一致；红头、发文字号、主送单位、标题、正文、附件、结束语、落款单位、矿名/部门名、日期逐项核对，并同步填写签发类型（外发文/内发文）和迎检资料备注。
4. 先做“是否有表格”的强制判断，再选择分支。旧 `.doc` 必须先复制并转换为工作 `.docx`，然后读取 `word/document.xml` 中真实的 `w:tbl` 数量；不能只凭正文出现“如下表”“表格”等文字判断。有 `w:tbl` 时走“有表格分支”；没有 `w:tbl` 时走“无表格分支”。若用户要求把纯文本改成表格，视为有表格任务；否则纯文字的“如下表”不触发表格分支。

5. 基础红头排版规则（两个分支都必须遵守）：A4；页边距目标 23 mm（±2）；红头使用宋体、45 磅、红色、加粗、居中；标题使用黑体二号、不加粗，段前、段后均为 0 行，采用 24 号模板的“正文文本/Normal”样式继承写法，不能套用“标题 3”或写成 0 磅、自动；正文仿宋四号、段前段后 0、固定值 27 磅（±2）；主送单位顶格，正文主体首行缩进 2 字符。24 号培训请示中的格式批注和文件有效字体继承关系作为字体依据；规则批注与正式定稿样式冲突时，优先保留用户提供的同类正式定稿样式，并在报告中指出差异。

6. 无表格分支：当 `w:tbl` 数量为 0 时，走“发表格问题之前”的稳定红头流程，只处理红头、发文字号、标题、主送、正文、序号、附件、落款、日期、页码和渲染验收；不得运行表格脚本，不得添加 `w:tblHeader`，不得为了预防表格问题写入表格分页、表格居中、行高、单元格段落或表格保护相关属性。验收重点是标题段前段后 0 行、正文 27 磅（±2）、页边距 23 mm（±2）、落款和页码；报告写明“未检测到真实表格，已走无表格分支”。

7. 有表格分支：当 `w:tbl` 数量大于 0 时，必须把文档拆成三个区域处理，禁止混用规则。

   - 公文区：红头、发文字号、标题、主送、正文段落、层级序号、附件、落款、日期、页码，以及不属于表格本体的普通段落，全部继续按基础红头公文规则调整。正文仍按仿宋四号、段前段后 0、固定值 27 磅（±2）、首行缩进 2 字符执行；标题仍按黑体二号、不加粗、段前段后 0 行执行。
   - 表前说明区：紧贴表格前的说明段，如“1.……如下表：”“附件：……表”等，按公文正文段落格式处理，但分页上要和紧随其后的表格看成一组；不得让说明段单独留在上一页、表格跑到下一页，也不得为了强行同页造成上一页大面积空白。
   - 表格本体区：从 `w:tbl` 开始到该表结束，进入表格保护模式。表格本体不套正文格式，不写入公文字体、正文行距、正文缩进或段前段后设置。

   表格保护模式是默认规则：不得向表格行、单元格或表格内段落写入字体、字号、底色、边框、行高、列宽、合并、文字、对齐、行不跨页、单元格段落行距或段前段后属性；不得删除或重写 `w:tblInd`、`w:jc`、`w:tblpPr`、`w:tblGrid`、`w:tblW`、`w:tcW`、`w:vMerge`、`w:gridSpan`、`w:tcBorders` 等表格结构和定位属性。现有实线边框必须完整保留；用户明确要求“表格必须都是实线”时，只允许把已有表格边框和斜线边框的线型统一为 `single` 实线，并在缺少必要网格线导致打印无实线时补齐最小 `w:tblBorders` / `w:tcBorders`；仍不得改变文字、字体、字号、底色、行高、列宽、合并和对齐。如果转换后实线边框、斜线或表头明显丢失，只能从套红头前的参考稿或用户确认的模板回填整张表格 XML，不得凭感觉重画或重建表格。若用户没有明确要求“新建表格”，不能把正文内容自动改造成表格。

   表格分页规则只允许做最小排版控制：

   1. 能整张表放在同一页时，优先让“表前说明 + 整张表”自然落在同一页；可以调整表格前后普通正文段落的分页关系，也可以在正文行距 27 磅（±2）范围内把表格前的公文正文压缩到 25 磅，或在页边距 23mm（±2）范围内微调上下页边距。不得通过改表格行高、列宽、字体、单元格行距、合并或边框来换取同页。
   2. 如果整张表无法放在同一页，允许跨页，但必须保留或添加表格抬头：优先使用原表首行作为重复表头，只向原首行写入 `w:tblHeader`；若原表已有明确标题行或多行表头，不得改变其文字和结构。不能为了重复表头复制出新的普通行，也不能把表格拆成多张独立表。
   3. 使用 `w:pageBreakBefore`、`w:keepNext` 或 `w:keepLines` 时，必须检查 `w:val`：标签存在且无 `w:val` 表示开启，`w:val="false"` 才表示关闭。硬分页只能用于防止“表前说明孤页/表头孤页”，不得造成大面积空白；渲染发现大空白时必须回退硬分页，改回自然排版或请求用户确认。
   4. 表格附近出现说明段或后续正文被挤到表格左侧、竖向逐字换行、与表格重叠等现象时，先判断是否由 `w:tblpPr` 浮动定位/文字环绕造成。默认仍不得删除表格定位属性；只有用户明确要求“修到可直接用”且候选渲染证明不会改变表格斜线、实线、表头和相对位置时，才可另存候选文件，对出问题表格外层定位做最小修复，并在报告中说明改了哪些外层属性。

   表格已经被用户指出“乱码、斜线变了、格式不对”时，应从套红头前的参考稿回填整张表格 XML，再继续处理公文区；若旧 WPS `.doc` 转 `.docx` 后斜线表头丢失，且首格可见文字合并为“审批岗位”“岗位审批”“备案天数岗位”等，允许只在该首格 `w:tcBorders` 内补回标准 `w:tl2br` 斜线边框，并必须校验去掉该标签后文档 XML 与补丁前一致。任何改动后都必须结构对比：表格文字、行数、单元格数、合并、列网格、斜线数量不得变化；边框验收必须单独列出“所有表格边框均为 `single` 实线”。确需改表格内部行高/行距/列宽时，必须先让用户明确确认。

8. 序号只按“一、”“（二）”“1.”“（1）”“①”逐级使用；序号较多时可在“一、”前加“第一章”。列表结束用“。”，不用“；”；书名号之间不加顿号。
9. 处理版式细节：正文内容上长下短；标题换行不得拆词；附件与结束语之间空一行；结束语“不加句号”；落款与上文空一行；“大饭铺煤矿”另起一行，不与“力量煤业有限公司”并排；日期用阿拉伯数字且单数字不补 0，置于公司名称居中位置；页码按模板在页底居中连续显示。
10. 生成新文件：已有 `.docx` 优先使用 [scripts/apply_red_header_docx.py](scripts/apply_red_header_docx.py) 做保守套用；再用 [scripts/validate_red_header_docx.py](scripts/validate_red_header_docx.py) 做结构检查，最后用 LibreOffice/WPS 渲染 PDF/PNG 做视觉检查。有表格文件必须额外记录表格数量和是否进入表格分支；无表格文件必须记录未运行表格处理。检查不通过时只做有规则依据的局部修正，并保留报告。

## 网页任务契约

网页办公助手会传入 `task_type=office_red_head_document`、`skill_name=red-header-official-document`。必须按以下约定处理：

- `payload.title`：文件标题；缺失时从上传原稿或正文首个标题段识别，不能识别则在验收报告列为待确认。
- `payload.documentNo` / `red_head_document.document_no`：发文字号；缺失时只能生成“待补文号审阅稿”。
- `payload.recipient` / `red_head_document.recipient`：主送单位；缺失时不得凭空推断。
- `payload.companyName` / `red_head_document.company_name`：红头单位，默认 `内蒙古准格尔旗力量煤业有限公司`。
- `payload.issueUnit` / `red_head_document.issue_unit`：落款单位；缺失时按红头单位临时处理并列为待确认。
- `payload.documentDate` / `red_head_document.document_date`：成文日期；缺失时列为待确认，不用当前日期冒充正式日期。
- `payload.signType`：`auto`、`external`、`internal`；用户手动选择优先于自动判断，但冲突必须写入报告。
- `payload.inspectionMaterial=true`：登记为迎检资料；备注位置未明确时只在报告中说明，不擅自塞进标题。
- `payload.outputMode`：`docxPdf`、`docxOnly`、`legacyDoc`。默认 `docxPdf`；请求旧 `.doc` 时必须先生成 `.docx` 并另存/转换。
- `payload.body` 与 `source_files[*].extracted_text_path`：正文来源。两者同时存在时，以上传原稿为主体，网页填写内容作为补充说明；冲突列为待确认。
- `payload.notes` / `red_head_document.notes`：经办人补充要求，例如文号、主送、落款、日期或内外发说明；只作为辅助要求，不当作正文。
- `payload.batchMode=true` 时表示用户在网页批量上传；网页会把每个 Word 原稿拆成独立任务，单个 `task.json` 仍只负责处理本任务的 `source_files`。`payload.batchIndex` 和 `payload.batchTotal` 仅用于报告说明。

处理网页任务前可运行 [scripts/validate_task.py](scripts/validate_task.py) 读取 `task.json`，把缺失字段、字段冲突和上传材料情况写入校验结果。任务完成后，必须在 `output_folder` 中生成可下载文件，并保留 `skill-run-metadata.json`。

## 输出边界

- 默认输出 `.docx` 和验收报告；`outputMode=docxPdf` 时同时输出 PDF 或页面预览。用户明确需要旧 `.doc` 时，最后一步另存/转换，并再次检查分页与字体。
- 发文字号缺失时只能生成“待补文号”审阅稿；取得正式文号后替换并重新验收，不能把占位稿当正式发文。
- 不把用户的原始公文、人员名单、联系方式或未脱敏附件复制进 skill；模板仅在用户明确要求长期内置且确认信任边界后才作为 assets 保存。
- 不把“未确认”标成“已满足”。最终报告至少列出：已满足规则、未能自动验证项、待用户确认项。
- 网页可见输出至少包含：套红头 DOCX、验收报告；如果生成失败，不得输出空白 DOCX 充数，必须让任务失败并写明原因。
